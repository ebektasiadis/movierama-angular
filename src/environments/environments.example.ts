export const environment = {
  // Set to true if you are deploying the app to a production environment.
  production: true,

  // Generate a key from https://www.themoviedb.org/settings/api and add the "API Read Access Token" here.
  movieDbApiKey: '',
};
